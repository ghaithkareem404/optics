# لوحة التحكم — دليل الإعداد (Admin Setup)

لوحة تحكم بسيطة على `/admin` ترفع منها موظفتك صور الموديلات تحت كل براند
(بدون قاعدة بيانات — الصور تُخزَّن على **Vercel Blob**).

## خطوات الإعداد (مرة واحدة، من لوحة Vercel)

### 1) فعّل تخزين الصور (Vercel Blob)
- افتح مشروعك في Vercel → تبويب **Storage** → **Create Database** → اختر **Blob** → **Create & Connect**.
- سيُضاف متغيّر البيئة `BLOB_READ_WRITE_TOKEN` تلقائياً.

### 2) أضف بيانات دخول الموظفة (Environment Variables)
اذهب إلى **Settings → Environment Variables** وأضف الثلاثة التالية (لكل البيئات):

| المتغيّر | القيمة |
| --- | --- |
| `ADMIN_USERNAME` | اسم مستخدم الموظفة (مثال: `zando-editor`) |
| `ADMIN_PASSWORD` | كلمة مرور قوية تعطيها للموظفة |
| `ADMIN_SESSION_SECRET` | نص عشوائي طويل (لتأمين الجلسة) |

> يمكنك استخدام هذه القيمة العشوائية الجاهزة لـ `ADMIN_SESSION_SECRET`:
> `f7e14913dffdf05334828238f7be826875db4c964bfb398fcac1ae29217aaf8e`
> (أو ولّد غيرها — أي نص طويل عشوائي يصلح.)

### 3) أعد النشر (Redeploy)
من تبويب **Deployments** → آخر نشر → **⋯ → Redeploy**، أو ادفع أي تعديل.

## كيف تستخدمها الموظفة
1. تفتح: `https://<موقعك>/admin`
2. تسجّل الدخول باسم المستخدم وكلمة المرور اللذين ضبطتهما.
3. تختار **البراند** (Optiswiss / Acuvue / Bella / Swarovski).
4. تكتب اسم الموديل (اختياري) ثم **تسحب الصورة أو تضغط للاختيار** → تُرفع فوراً.
5. تظهر الصور في صفحة **المنتجات** بالموقع تحت البراند المناسب.
6. لحذف صورة: تمرّ فوقها وتضغط زر الحذف 🗑️.

## ملاحظات
- **لا تحتاج الموظفة حساباً على Vercel** — تدخل على `/admin` بالموقع فقط.
- لتغيير كلمة مرور الموظفة أو إلغاء وصولها: عدّل `ADMIN_PASSWORD` في Vercel وأعد النشر.
- الصور المرفوعة عامة (لعرضها في الموقع)؛ الرفع والحذف محميّان بتسجيل الدخول.
- الأنواع المسموحة: JPG / PNG / WEBP حتى 8MB لكل صورة.

## التطوير محلياً (اختياري)
أنشئ ملف `.env.local`:
```
BLOB_READ_WRITE_TOKEN=... (من Vercel Blob)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
ADMIN_SESSION_SECRET=any-long-random-string
```
