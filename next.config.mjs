/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add remote image hosts here when you switch from local placeholder art to a CMS/CDN.
    remotePatterns: [],
  },
};

export default nextConfig;
